<% if notice.present? || alert.present? %>
  <div class='fixed top-[2rem] z-[60] w-screen flex justify-center' id='flash-message' role='alert'>
    <div class="flex bg-white dark:bg-gray-900 text-gray-700 dark:text-gray-300 flex-row shadow-md border border-gray-100 rounded-lg overflow-hidden md:w-2/4 lg:w-2/4 sm:mx-4">
      <div class="flex w-3 bg-gradient-to-t <%= flash[:notice] ? 'from-green-500 to-green-400 dark:from-green-600 dark:to-green-500' : 'from-red-500 to-red-400 dark:from-red-600 dark:to-red-500' %>"></div>
      <div class="flex-1 p-3">
        <p class="font-light"><%= notice || alert %></p>
      </div>
      <button type="button" class="cursor-pointer border-l hover:bg-gray-50 dark:hover:bg-gray-800 border-gray-100 px-4 flex place-items-center" data-dismiss-target="#flash-message" aria-label="Close">
        <span class="sr-only">Dismiss</span>
        <p class="text-xs">CLOSE</p>
      </button>
    </div>
  </div>
<% end %>
